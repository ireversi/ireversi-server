tags:
  - name: kai-playing
    description: Kai's Board API

/kai/board:
  get:
    tags:
      - kai-playing
    externalDocs:
      description: "Jump directly to JSON"
      url: "https://ireversi-server.now.sh/api/v1/kai/board"
    responses:
      200:
        description: "success"
        schema:
          type: "array"
          items:
            type: object
            properties:
              x:
                type: "integer"
                format: "int64"
                example: 1
              y:
                type: "integer"
                format: "int64"
                example: 1
              userId:
                type: "integer"
                format: "int64"
                example: 1
