tags:
  - name: kai-board
    description: Kai's Board API

/kai/board:
  get:
    tags:
      - kai-board
    responses:
      200: # 404的なやつ
        description: "success"
        schema:
          type: "array"
          items:
            type: object
            properties:
              x:
                type: "integer"
                format: "int64"
                example: 1
              y:
                type: "integer"
                format: "int64"
                example: 1
              userId:
                type: "integer"
                format: "int64"
                example: 1
